{
  "name": "web-app",
  "version": "1.0.0",
  "description": "Front-end for the web-dev-roadmap",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Esteban Duran",
  "license": "ISC",
  "dependencies": {
    "core-js": "^3.9.0",
    "regenerator-runtime": "^0.13.7"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  }
}
